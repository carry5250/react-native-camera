/**
 * This code was generated by "react-native codegen-lib-harmony"
 */

import {
  Descriptor as ComponentDescriptor,
  ViewBaseProps,
  ViewRawProps,
  ViewDescriptorWrapperBase,
  ColorValue,
  Color,
  RNInstance,
  Tag,
  RNComponentCommandReceiver,
  ViewPropsSelector,
} from '@rnoh/react-native-openharmony/ts';


export namespace RTNCameraView {
  export const NAME = "RTNCameraView" as const

  export interface DirectRawProps {
    zoom?: number;
    useNativeZoom?: boolean;
    maxZoom?: number;
    ratio?: string;
    focusDepth?: number;
    type?: string;
    faceDetectionMode?: number;
    trackingEnabled?: boolean;
    flashMode?: 'auto' | 'on' | 'off';
    exposure?: number;
    barCodeTypes?: string[];
    googleVisionBarcodeType?: number;
    googleVisionBarcodeMode?: number;
    whiteBalance?: number;
    faceDetectionLandmarks?: number;
    autoFocus?: string;
    autoFocusPointOfInterest?: {x: number, y: number};
    faceDetectionClassifications?: number;
    captureAudio?: boolean;
    keepAudioSession?: boolean;
    useCamera2Api?: boolean;
    playSoundOnCapture?: boolean;
    playSoundOnRecord?: boolean;
    videoStabilizationMode?: number;
    pictureSize?: string;
    rectOfInterest: {x: number, y: number, width: number, height: number};
    isAuthorized: boolean;
    isAuthorizationChecked: boolean;
    recordAudioPermissionStatus?: 'AUTHORIZED' | 'NOT_AUTHORIZED' | 'PENDING_AUTHORIZATION';
    text?: string;
    color?: ColorValue;
  }
  
  export interface Props extends ViewBaseProps {}
  
  export interface State {}
  
  export interface RawProps extends ViewRawProps, DirectRawProps {}
  
  export class PropsSelector extends ViewPropsSelector<Props, RawProps> {
    get zoom() {
      return this.rawProps.zoom ?? 0;
    }
    
    get useNativeZoom() {
      return this.rawProps.useNativeZoom ?? true;
    }
    
    get maxZoom() {
      return this.rawProps.maxZoom ?? 0;
    }
    
    get ratio() {
      return this.rawProps.ratio;
    }
    
    get focusDepth() {
      return this.rawProps.focusDepth ?? 0;
    }
    
    get type() {
      return this.rawProps.type;
    }
    
    get faceDetectionMode() {
      return this.rawProps.faceDetectionMode ?? 0;
    }
    
    get trackingEnabled() {
      return this.rawProps.trackingEnabled ?? false;
    }
    
    get flashMode() {
      return this.rawProps.flashMode ?? 'auto';
    }
    
    get exposure() {
      return this.rawProps.exposure ?? 0;
    }
    
    get barCodeTypes() {
      return this.rawProps.barCodeTypes;
    }
    
    get googleVisionBarcodeType() {
      return this.rawProps.googleVisionBarcodeType ?? 0;
    }
    
    get googleVisionBarcodeMode() {
      return this.rawProps.googleVisionBarcodeMode ?? 0;
    }
    
    get whiteBalance() {
      return this.rawProps.whiteBalance ?? 0;
    }
    
    get faceDetectionLandmarks() {
      return this.rawProps.faceDetectionLandmarks ?? 0;
    }
    
    get autoFocus() {
      return this.rawProps.autoFocus;
    }
    
    get autoFocusPointOfInterest() {
      return this.rawProps.autoFocusPointOfInterest;
    }
    
    get faceDetectionClassifications() {
      return this.rawProps.faceDetectionClassifications ?? 0;
    }
    
    get captureAudio() {
      return this.rawProps.captureAudio ?? false;
    }
    
    get keepAudioSession() {
      return this.rawProps.keepAudioSession ?? false;
    }
    
    get useCamera2Api() {
      return this.rawProps.useCamera2Api ?? false;
    }
    
    get playSoundOnCapture() {
      return this.rawProps.playSoundOnCapture ?? false;
    }
    
    get playSoundOnRecord() {
      return this.rawProps.playSoundOnRecord ?? false;
    }
    
    get videoStabilizationMode() {
      return this.rawProps.videoStabilizationMode ?? 0;
    }
    
    get pictureSize() {
      return this.rawProps.pictureSize;
    }
    
    get rectOfInterest() {
      return this.rawProps.rectOfInterest;
    }
    
    get isAuthorized() {
      return this.rawProps.isAuthorized ?? false;
    }
    
    get isAuthorizationChecked() {
      return this.rawProps.isAuthorizationChecked ?? false;
    }
    
    get recordAudioPermissionStatus() {
      return this.rawProps.recordAudioPermissionStatus ?? 'AUTHORIZED';
    }
    
    get text() {
      return this.rawProps.text;
    }
    
  
    get color() {
        if (this.rawProps.color) {
          return Color.fromColorValue(this.rawProps.color)
        } else {
          return new Color({ r: 0, g: 0, b: 0, a: 255})
        }
    }
    
  }

  export type Descriptor = ComponentDescriptor<
    typeof NAME,
    Props,
    State,
    RawProps
  >;
  
  export class DescriptorWrapper extends ViewDescriptorWrapperBase<
    typeof NAME,
    Props,
    State,
    RawProps,
    PropsSelector
  > {
    protected createPropsSelector() {
      return new PropsSelector(this.descriptor.props, this.descriptor.rawProps)
    }
  }
  
  export interface EventPayloadByName {
    "textTouch": {type: number}
  }
  
  export class EventEmitter {
    constructor(private rnInstance: RNInstance, private tag: Tag) {}
    
    emit<TEventName extends keyof EventPayloadByName>(eventName: TEventName, payload: EventPayloadByName[TEventName]) {
      this.rnInstance.emitComponentEvent(this.tag, eventName, payload)
    }
  }
  
  export interface CommandArgvByName {
  }
  
  export class CommandReceiver {
    private listenersByCommandName = new Map<string, Set<(...args: any[]) => void>>()
    private cleanUp: (() => void) | undefined = undefined
  
    constructor(private componentCommandReceiver: RNComponentCommandReceiver, private tag: Tag) {
    }
  
    subscribe<TCommandName extends keyof CommandArgvByName>(commandName: TCommandName, listener: (argv: CommandArgvByName[TCommandName]) => void) {
      if (!this.listenersByCommandName.has(commandName)) {
        this.listenersByCommandName.set(commandName, new Set())
      }
      this.listenersByCommandName.get(commandName)!.add(listener)
      const hasRegisteredCommandReceiver = !!this.cleanUp
      if (!hasRegisteredCommandReceiver) {
        this.cleanUp = this.componentCommandReceiver.registerCommandCallback(this.tag, (commandName: string, argv: any[]) => {
          if (this.listenersByCommandName.has(commandName)) {
            const listeners = this.listenersByCommandName.get(commandName)!
            listeners.forEach(listener => {
              listener(argv)
            })
          }
        })
      }
  
      return () => {
        this.listenersByCommandName.get(commandName)?.delete(listener)
        if (this.listenersByCommandName.get(commandName)?.size ?? 0 === 0) {
          this.listenersByCommandName.delete(commandName)
        }
        if (this.listenersByCommandName.size === 0) {
          this.cleanUp?.()
        }
      }
    }
  }

}
